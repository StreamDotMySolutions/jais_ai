WWEBJS_TEST_REMOTE_ID=XXXXXXXXXX@c.us
WWEBJS_TEST_CLIENT_ID=authenticated
WWEBJS_TEST_MD=1

API_URL=https://jais.jebatderhaka.com/api/whatsappweb
CHROMIUM_PATH=/usr/bin/chromium-browser

# Laravel internal status ingest endpoint. Should be loopback in prod.
WAWEB_STATUS_URL=http://127.0.0.1:8000/api/whatsappweb/_internal/status
# Must match WAWEB_INTERNAL_SECRET on the Laravel side.
WAWEB_INTERNAL_SECRET=change-me-long-random-string
# Port that the bridge's control server listens on (loopback only).
# Laravel POSTs to http://127.0.0.1:${WAWEB_CONTROL_PORT}/logout to force a re-pair.
WAWEB_CONTROL_PORT=3001